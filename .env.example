# Optional. When set, PLAiChat upgrades from the built-in deterministic parser
# to an LLM that maps natural-language questions to the same query schema.
# The app is fully functional WITHOUT this key (deterministic fallback).
ANTHROPIC_API_KEY=
